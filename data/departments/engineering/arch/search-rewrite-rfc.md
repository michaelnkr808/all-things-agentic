# RFC — Search Rewrite

**Status:** Draft. Scoped at 6 engineer-weeks, not yet funded.

Replaces the LIKE-based search with an inverted index. Blocked on the Q4
budget decision — the estimate assumes one additional backend engineer, and
that hire slipped (see hiring-plan.md).
